{% comment %}
  Local override for al_folio_cv education entries.
  Keep dates in the left column and place locations with the institution.
{% endcomment %}

<ul class="card-text font-weight-light list-group list-group-flush cv-list-clean">
  {% for entry in entries %}
    <li class="list-group-item">
      {% assign start_date = entry.start_date | default: entry.startDate | strip %}
      {% assign end_date = entry.end_date | default: entry.endDate | strip %}
      {% assign start_year = start_date | split: '-' | first | strip %}
      {% assign end_year = end_date | split: '-' | first | strip %}
      {% if end_year == 'present' %}
        {% assign end_year = 'Present' %}
      {% endif %}
      {% if start_year != '' %}
        {% if end_year == '' %}
          {% assign end_year = 'Present' %}
        {% endif %}
        {% assign date = start_year | append: ' - ' | append: end_year %}
      {% else %}
        {% assign date = '' %}
      {% endif %}

      {% assign study_type = entry.studyType | default: entry.degree | strip %}
      {% assign location = entry.location | strip %}

      <div class="row cv-entry-row">
        <div class="col-12 col-sm-3 col-md-2 cv-date-column">
          {% if date != '' %}
            <span class="badge font-weight-bold danger-color-dark text-uppercase align-middle">{{ date }}</span>
          {% endif %}
        </div>
        <div class="col-12 col-sm-9 col-md-10 cv-detail-column">
          {% if entry.url %}
            <h6 class="title font-weight-bold">
              <a href="{{ entry.url }}">{{ study_type }}</a>
            </h6>
          {% elsif study_type != '' %}
            <h6 class="title font-weight-bold">{{ study_type }}</h6>
          {% endif %}

          {% if entry.institution or location != '' %}
            <p class="cv-meta">
              {% if entry.institution %}{{ entry.institution }}{% endif %}
              {% if entry.institution and location != '' %}<span class="cv-meta-separator">&middot;</span>{% endif %}
              {% if location != '' %}
                <span class="cv-location"><i class="fa-solid fa-location-dot iconlocation"></i> {{ location }}</span>
              {% endif %}
            </p>
          {% endif %}

          {% if entry.area %}
            <p class="cv-summary">{{ entry.area }}</p>
          {% endif %}

          {% if entry.courses or entry.highlights %}
            <ul class="items">
              {% for item in entry.courses %}
                <li>
                  <span class="item">{{ item }}</span>
                </li>
              {% endfor %}
              {% for item in entry.highlights %}
                <li>
                  <span class="item">{{ item | markdownify | remove: '<p>' | remove: '</p>' }}</span>
                </li>
              {% endfor %}
            </ul>
          {% endif %}
        </div>
      </div>
    </li>
  {% endfor %}
</ul>
